version: 1
trail:
  artifacts:
    - name: application
      attestations:
        - name: unit-tests
          required: true
    - name: docker-image
      attestations:
        - name: sbom
          required: true